"""Evaluation harness: baseline conditions and the experiments that compare them.

This package is deliberately outside `src/warnsync_mcp`. The baselines exist to
be measured against, not to be shipped, and keeping them here means the served
system carries no evaluation scaffolding.
"""
